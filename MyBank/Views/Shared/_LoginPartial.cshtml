@using Microsoft.AspNetCore.Identity
@using MyBank.Persistence.Dao

@inject SignInManager<Customer> SignInManager
@inject UserManager<Customer> UserManager
@inject Microsoft.AspNetCore.Http.IHttpContextAccessor Accessor

@if (SignInManager.IsSignedIn(User))
{
	<!--<form asp-area="" asp-controller="Account" asp-action="Logout" method="post" id="logoutForm" class="nav-link text-dark navbar-right">
		<ul class="nav navbar-nav navbar-right">
			<li class="nav-item">
				<h5 class="nav-link text-dark font-weight-bold">Üdvözöljük @UserManager.GetUserName(User)!</h5>
			</li>
			<li class="nav-item">
				<button type="submit" class="btn nav-link text-dark">Kijelentkezés</button>
			</li>
		</ul>
	</form>-->
}
else
{
	<ul class="nav navbar-nav navbar-right">
		<li>
			<a asp-area="" asp-controller="Account" asp-action="Register" class="nav-link text-dark @(Accessor.HttpContext.Request.Path.Value == "/Account/Register" ? "active" : "border-animation" )" style="margin-left: 5px; margin-right: 5px">
				Regisztrálás
			</a>
		</li>
		<li>
			<a asp-area="" asp-controller="Account" asp-action="Login" class="nav-link text-dark @(Accessor.HttpContext.Request.Path.Value == "/Account/Login" ? "active" : "border-animation" )" style="margin-left: 5px; margin-right: 5px" >
				Bejelentkezés
			</a>
		</li>
	</ul>
}